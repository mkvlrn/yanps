{
  "name": "$NAME$",
  "scripts": {
    "build": "rimraf ./dist && babel ./src -d ./dist --copy-files",
    "dev": "babel-node ./src",
    "lint": "eslint src/**/*.js",
    "test": "jest"
  },
  "devDependencies": {},
  "dependencies": {}
}
