{
  "name": "$NAME$",
  "scripts": {
    "build": "rimraf ./dist && webpack --mode production",
    "dev": "webpack serve --mode development",
    "lint": "eslint src/**/*.js",
    "test": "jest"
  },
  "devDependencies": {},
  "dependencies": {}
}
